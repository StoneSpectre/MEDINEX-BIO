# ── Core graph driver ─────────────────────────────────────────
neo4j>=5.20.0
requests>=2.31.0
pandas>=2.2.0
python-dotenv>=1.0.1
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
anthropic>=0.28.0
qdrant-client>=1.9.1
sentence-transformers>=2.7.0
torch>=2.3.0

# ── XML parsing (PubMed efetch — stdlib, listed for clarity) ───
# xml.etree.ElementTree  (stdlib — no install needed)
